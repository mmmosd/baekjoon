#include <stdio.h>

int tc;

int main() {
    scanf("%d", &tc);

    for (int t; = 0; t < tc; t++) {
        int ary[1000005] = {0,};
        int n;
        scanf("%d", &n);

        for (int i = 0; i < n; i++) {
            scanf("%d", &ary[i]);
        }

        
    }
}