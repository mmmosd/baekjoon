#include <stdio.h>
#include <vector>
using namespace std;

typedef long long ll;

vector<ll> segment;

ll ary[500005];
int n;

int main() {
	scanf("%d", &n);
	
	for (int i = 0; i < n; i++) {
		scanf("%d", &ary[i]);
	}
}
