#include <stdio.h>

int main() {
    int n, k;
    scanf("%d%d", &n, &k);
    for (int i = 0; i < n; i++) {
        int m, v;
        scanf("%d%d", &m, &v);
    }
    for (int i = 0; i < k; i++) {
        int c;
        scanf("%d", &c);
    }
}