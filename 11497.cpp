#include <stdio.h>

int t;

int main() {
    scanf("%d", &t);

    for (int tc = 0; tc < t; tc++) {
        int ary[10005] = {0,};
        int n;
        scanf("%d", &n);

        for (int i = 0; i < n; i++) {
            scanf("%d", &ary[i]);
        }

        
    }
}