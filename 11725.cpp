#include <stdio.h>

int tree[400005];

int n;

int main() {
	scanf("%d", &n);
	
	for (int i = 0; i < n; i++) {
		int a, b;
		scanf("%d%d", &a, &b);
	}
	
	for (int i = 0; i < n; i++) {
		printf("%d\n", );
	}
}
