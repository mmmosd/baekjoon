#include <stdio.h>

int tc;

int main() {
    scanf("%d", &tc);

    for (int t = 0; t < tc; t++) {
        int target;

        int n, k;
        scanf("%d%d", &n, &k);

        for (int i = 0; i < n; i++) {
            int cost;
            scanf("%d", &cost);
        }

        for (int i = 0; i < k; i++) {
            int a, b;
            scanf("%d%d", &a, &b);
        }

        scanf("%d", &target);

        
    }
}